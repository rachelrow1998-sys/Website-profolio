# 作品集网站 — 完整需求 Brief

> 这份文件是这个项目所有设计决策的记录。
> 动效编排另见 **`MOTION.md`**。
> 可以整份复制给任何 AI 工具（Claude / Cursor / Lovable / v0…），也可以交给接手的人。
> 想改方向时，**先改这份文件，再改代码** —— 不然过两个月你会忘记当初为什么这样定。

---

## 1. 一句话

给一个马来西亚独立接单的网页设计师，做一个在线作品集网站，
用来取代「在 WhatsApp 里一条条贴链接给客户」这件事。

---

## 2. 背景（这部分决定了后面所有取舍，不要跳过）

**我是谁**：JH，马来西亚独立网页设计师 / 开发者，一个人负责设计、开发、上线。

**客户从哪来**：
- 小红书（主要）—— 讲中文，年轻，看视觉
- 马来西亚本地企业 —— 讲英文 / 马来文，年纪偏大，看专业度
- 老客户转介绍

**客户是什么行业**：建筑工程、建材、五金 DIY、宠物、教育、健康护理、OEM 代工、会员制品牌。
行业差别很大，但他们要的东西一样：**一眼看懂、一眼相信、然后主动联系我**。

**现在的痛点**：每次有人问「你做过什么」，我就丢 10 条链接过去。
客户不会一条条点开，也看不出这些是不是同一个人做的。

**这个网站要解决的**：一个网址，客户自己看完，然后按 WhatsApp 找我。

---

## 3. 核心设计原则（最重要的一条，其他都可以商量）

> **整个页面保持中性无彩。页面上唯一有颜色的东西，是作品截图。**

理由：我卖的就是那些截图。页面自己有颜色 = 跟自己的作品抢注意力。
画廊的墙是白的，不是因为没钱刷漆，是因为墙不该跟画争。

**这一条推导出**：
- 底色用纸色，文字用墨色，分隔用细线
- 主按钮是黑底白字，不是彩色
- 作品截图用卡纸边框 + 细线框 + 投影，像照片摊在桌上
- 全站唯一允许的彩色 UI 是右下角 WhatsApp 悬浮球（因为它必须被找到）

**踩过的坑**：第一版做的是深蓝发光的游戏菜单风格。10 张彩色截图放在会发光的蓝卡片上，
旁边还有红圆盘和蓝光晕 —— 结果页面上没有任何一个元素比另一个安静，客户不知道该看哪。
**很酷，但卖不出去东西。**

---

## 4. 视觉规范

### 颜色（全部）

```css
--paper:   #EDEAE3;   /* 纸 */
--paper-2: #E5E1D7;   /* 稍深的纸，用于分区 */
--mat:     #F6F4EF;   /* 作品图的卡纸边 */
--ink:     #14120F;   /* 墨 */
--ink-2:   #45403A;   /* 次级文字 */
--pencil:  #8A8478;   /* 铅笔灰，标签和辅助信息 */
--rule:    #CEC8BC;   /* 细线 */
--rule-2:  #DCD7CC;   /* 更淡的细线 */
--seal:    #A8341F;   /* 印章红，只用在 hover 的编号上 */
--wa:      #1FA855;   /* 只用在右下角悬浮按钮 */
```

### 字体

- 标题 / 数字：**Instrument Serif**（高对比衬线，编辑感）
- 界面 / 正文：**Archivo**（几何无衬线，小字加大字距时有制图感）
- 中文：走系统字体，衬线场景用 `Songti SC / SimSun / Noto Serif CJK SC`

**必须自托管**（放 `assets/fonts/`，不要用 Google Fonts CDN）。
原因：客户从小红书来，**在中国大陆 Google Fonts 是打不开的**，
字体加载不出来标题会掉回系统默认，整个设计当场垮掉。
只保留 latin 子集（英文 + 马来文够用），中文字体动辄几 MB，不值得让访客下载。
两款字体都是 SIL Open Font License，自托管和商用都合法。目前总共 72KB。

### 质感

- 纸张颗粒：SVG `feTurbulence` 生成，`mix-blend-mode: multiply`，低透明度
- 封面有套准标记（四角十字线）+ 极淡的制图方格底纹
- 标题下面一道墨迹笔触：**用 SVG 路径过湍流滤镜画出来，不要用假的笔刷字体**（一眼假）
- 标题后面一个更大的、几乎看不见的重影字

---

## 5. 页面结构（单页，从上往下）

### 顶栏（固定）
左：`JH` 缩写 + 「网页设计」/ 中：四个标签（档案 / 作品 / 服务 / 联系）/ 右：语言切换 + WhatsApp 按钮
- 标签用黑色滑块指示当前区块，滑块要跟着滚动平滑移动
- 滚动超过 20px 后加毛玻璃背景

### 1. 封面
- `JH STUDIO's`（小字大字距）
- 巨大的 `Portfolio`（衬线体）+ 下面那道墨迹
- `2023 — 2026　—　精选作品`
- 四个编号入口：`01 作品 / 02 关于 / 03 服务 / 04 联系`（细线分隔的横排）
- 底部：客户名字跑马灯

### 2. 作品（最重要的一屏）
- 标题 + 一句话（「每一个都点得进去，是真站，不是效果图。」）
- 四个数字：上线项目 / 服务行业 / 年经验 / 手机端适配（滚到才开始跳动）
- 行业筛选按钮（细线连排，选中的变黑底白字）
- 作品网格：每张卡片 = 首页截图 + 名字 + 一句话 + 工作内容标签 + 「查看详情」
- 点卡片进 **Case Study Focus Mode**（整页作品详情，不是弹窗）：
  大图 + 挑战 / 我的工作 / 成果 + 服务范围 + 「打开网站」，
  底部可以直接翻到其他作品，关掉时图片飞回原来那张卡的位置

### 3. 关于
- 照片（竖构图，卡纸边框）
- 两段自我介绍
- 常用软件（图标方块，纸质版**刻意不着色**）
- 教育背景 / 经历（细线列表）
- 语言能力（细线进度条，滚到才画出来）

### 4. 服务
六项，编号 01–06，细线分格：
企业形象官网 / 品牌落地页 / 电商与产品目录 / 询盘与预约表单 / 速度与 SEO 基础 / 后续维护与更新

### 5. 流程
四步：免费咨询 → 设计确认 → 开发制作 → 上线与交付

### 6. 结尾
左：头像 + 名字 + 联系方式列表 / 右：巨大的「谢谢观看」+ 2026 + WhatsApp 主按钮 + 发邮件

---

## 6. 功能要求

### 三语切换（中文 / English / Bahasa Melayu）
- 右上角一键切换，**自动跟随访客浏览器语言**，选择记进 localStorage
- 小红书来的看中文，本地客户和 Google 搜索看英文，马来客户看马来文
- 所有文案集中在一个 `i18n.js`，改文字不用碰结构

### WhatsApp 优先
- 顶栏按钮 + 结尾主按钮 + 右下角悬浮球，三处
- 点了直接开对话，并且**自动带一句开场白**（三种语言各一句）
- 号码格式：国码+号码，不要 `+`、空格、短横线
- 号码没填时按钮滚到联系区，不要把客户丢去空的 WhatsApp

### 内容全部数据驱动
- `data.js`：我的资料 + 作品列表（含作品详情页文案）+ 个人档案 —— **只改这一个文件就能更新内容**
- 每个作品的 `study` 里有：副标题 / 挑战 / 我的工作 / 成果 / 服务范围，三语各一份
- ⚠️ `study.result`（成果）全部是留白提示，需要你填真实数字或客户原话。
  我不会替你写「预约量提升 182%」这种编出来的业绩 —— 客户一追问就会当场被问倒
- `i18n.js`：三语文案
- 首页那几个数字（项目数 / 行业数 / 年资）**自动算出来**，加作品会自己更新
- 分类筛选按钮**自动生成**，没作品的分类自动隐藏

### 作品截图
- 一条命令自动抓所有网站首页（Playwright），存成 `assets/screenshots/<slug>.jpg`
- 截图没放进去时**自动显示占位图**，绝对不能出现破图
- 脚本要能处理：等懒加载、关掉 cookie 弹窗、失败了告诉我哪几个失败该怎么办

---

## 7. 动效

> **📄 完整编排见 `MOTION.md`** —— 开场序列、Hero 卡片群、Flip 空间转换、
> 十个项目各自的 reveal personality、逐屏时间轴，全部在那份文件里。
> 下面只是基线要求。

### 每个动画都要回答一个问题

> **它是在展示作品，解释层级，还是帮助导航？**
> **三个都不是 —— 删。**

这条决定了「animation 很多」不会变成「所有东西都在动」。
客户应该感受到「这个作品集很会动」，而不是「所有东西一直在动」。

### 关于动画库的决定（已修订）

**原本的决定**：不引入任何库，全部手写。

**修订**：`MOTION.md` 那套编排里有三处 Flip 空间转换和一处 pin/scrub，
手写成本明显高于引入成本，改用 GSAP —— **但只用在这四处**：

1. Hero 卡片群 → Works Grid（`Flip`）
2. Works 筛选重排（`Flip`）
3. Works Grid → Case Study，以及关闭时飞回原位（`Flip`）
4. Case Study 内部有限的 pin / scrub，仅桌面（`ScrollTrigger`）

其他一律 CSS / 原生 JS / WAAPI：开场序列、墨迹、数字滚动、光标、磁吸、
鼠标视差、跑马灯、普通 reveal、惯性滚动。

**标题拆字也不引入 SplitText** —— 在渲染阶段生成 `<span>` word wrapper 即可
（`main.js` 里已经手写好了）。

> 库是为了处理困难的问题，不是因为「这是动画」就全部交给库。

**⚠️ 关于体积 —— 我之前写错过一次**：
早前我在文档里写了「≈40KB gzip」这样的具体数字。**那个写法是错的** ——
官方没有把这些 gzip 数字作为固定规格公布，而且会随版本变动。
**以我们实际部署版本的 build / Network 实测为准**，测完把数字和测量日期记进 `MOTION.md` 1.2 节。

### Mobile 是一级需求，不是降级方案

**不是**「Desktop 做完 → `if (mobile) disableAnimation()`」，
**而是 Desktop choreography 和 Mobile choreography 两份独立设计**。

手机上完全不做：`pin` · `scrub` · `mouse parallax` · `magnetic` · `3D tilt` · `Flip`

手机是大部分客户第一次打开这个网站的设备（小红书点链接过来就是手机）。
目标：**5–10 秒内已经看到作品**。具体编排见 `MOTION.md` 第 3 节。

### 转化路径优先

客户**不需要**看完 10 个 Case Study 才能按 WhatsApp。
主路径保持很短：`Opening → Hero → Works Grid → About → Services → Contact`。
Case Study 是**点进去**的 Focus Mode，不是必经关卡。

### 基线要求（不管用不用库都要满足）
- 惯性平滑滚动（那种有重量感的滚动）
- 滚动速度倾斜（滚得快画面微微倾斜，幅度控制在 ±2 度）
- 视差（照片跟滚动错位移动）
- 标题字符逐个入场（**按词分组，词内部不能断行**，否则会出现 `PORTFOLI / O`）
- 磁吸按钮（鼠标靠近被吸过去）
- 自定义光标（圆环延迟跟随，碰到可点元素放大）
- 作品卡片悬浮时轻轻抬起并微微转一点角度，像把照片拿起来看
- 数字滚动 + 进度条（进入视野才开始）
- 开机载入动画

**必须自动关掉的场景**：
- 手机 / 触屏：关掉平滑滚动、光标、磁吸、3D 倾斜（触屏上这些只会碍事）
- 系统开了「减少动态效果」：全部关掉
- **兜底**：不管发生什么，4 秒后所有内容一定可见（JS 挂了也不能白屏）

---

## 8. 技术要求

- 纯 HTML / CSS / JS，**不要框架、不要 build 步骤**，免费部署到 Netlify / GitHub Pages
- **零外部请求** —— 字体、图片、样式、脚本全部自己托管
- 手机、平板、电脑全适配，任何宽度都不能横向溢出
- SEO：完整 meta、OG 分享卡片、语义化标签
- 键盘无障碍：Tab 能走完全站，焦点元素必须自动滚进视野
  （**注意**：如果用 `position: fixed` 做平滑滚动容器，浏览器自带的「滚动到焦点」会失效，必须自己实现）
- 所有用户数据渲染进 HTML 前必须转义（否则像 `</>` 这种内容会被当成标签吃掉）
- 打包成单个 HTML 文件的脚本（发邮件给客户用）
  - **坑**：JS 的 `String.replace` 里 `$$` 是转义符，会把代码里的 `$$()` 悄悄改成 `$()`。必须用函数式替换。

---

## 9. 内容清单（当前 10 个作品）

| # | 名称 | 网址 | 分类 | 年份 |
|---|------|------|------|------|
| 01 | The Luma Club | thelumaclub.com | 品牌·生活 | 2026 |
| 02 | EXA Energy Sdn. Bhd. | exaenergy.asia | 工业·建材 | 2025 |
| 03 | PNC Lifecare | pnclifecare.com | 品牌·生活 | 2025 |
| 04 | Furfoo Pet | furfoopet.com | 电商·零售 | 2024 |
| 05 | YH Ideal Academy | yhidealacademy.com | 教育 | 2024 |
| 06 | MITIC Asian | miticasian.org | 企业官网 | 2024 |
| 07 | ETAETA | etaeta.co | 品牌·生活 | 2024 |
| 08 | EC DIY Hardware | ecdiyhardware.com.my | 电商·零售 | 2023 |
| 09 | OEM4U2DAY | oem4u2day.com | 企业官网 | 2023 |
| 10 | Master Materials | mastermaterials.com.my | 工业·建材 | 2023 |

分类只有五个：`corporate` / `industrial` / `ecommerce` / `education` / `lifestyle`

每个作品要写：**我帮这个客户解决了什么问题**，不要写「这是一个好看的网站」。
客户看作品集，看的是「这个人能不能解决我的问题」。

---

## 10. 明确不要做的

- ❌ **不要用现成模板**。客户看我的作品集，本身就是在看我的设计水平。用模板等于第一印象就说「我用模板」。
- ❌ **不要用 GitHub 上那些 developer portfolio 模板**。那些是程序员求职用的（放 GitHub 项目、技术栈、算法题），跟「接单设计师给客户看」是两回事。
- ❌ **不要给页面加彩色**。每加一个彩色元素，都会开始跟作品截图抢注意力。
- ❌ **不要用 Google Fonts CDN**。中国大陆打不开。
- ❌ **不要用假的笔刷字体**做墨迹效果，一眼假。
- ⚠️ **动画库**：只有 `MOTION.md` 1.3 节列的四处可以用 GSAP，其余一律不用。判断标准：**手写这件事是不是明显更贵**。
- ❌ **不要用 AI mockup 当最终素材**。mockup 只负责确定 composition / motion direction，production 必须抓真实网站截图 —— 动画围绕真实作品做，不是让作品迁就 mockup。
- ❌ **不要写「这是一个响应式的现代化网站」这种废话文案**。客户不关心，客户只关心你能不能帮他赚钱。

---

## 11. 验收标准

- [ ] 10 个作品卡片全部显示真实首页截图，点得进去
- [ ] 三种语言切换正常，所有文字都跟着变
- [ ] 手机上没有横向滚动条
- [ ] WhatsApp 按钮点了能开对话，并且带上开场白
- [ ] 网络面板里**零个外部域名请求**
- [ ] 关掉 JS，内容依然看得见（SEO 和无障碍）
- [ ] 用键盘 Tab 走一遍，焦点不会跑到看不见的地方
- [ ] 系统开「减少动态效果」后，所有动画停止
- [ ] 页面总重量（不含作品截图）控制在 250KB 以内
- [ ] 把网址发给一个没见过的人，他能在 30 秒内说出「这个人是做什么的、做过什么、怎么联系」

---

## 12. 还没做完的

- [ ] 放我的照片进 `assets/img/me.jpg`（这个风格很吃照片，黑白或低饱和的最搭）
- [ ] 填真实 WhatsApp 号码和邮箱
- [ ] 填 `PROFILE` 里的教育背景 / 经历 / 语言能力（现在是示例值）
- [ ] 跑 `npm run shots` 抓 10 个网站截图
- [ ] 改作品介绍文案（现在是按行业猜写的草稿，只有我自己知道真实细节）
- [ ] 上线（Netlify，子域名取名 `jhstudio.netlify.app` 之类）
